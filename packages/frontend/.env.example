# Calla frontend — copy to `.env` (or `.env.local`) to override.

# Run on local mock/seed data (default). Set to "false" to call the live backend.
VITE_USE_MOCK=true

# ContinuaCare backend base URL (used only when VITE_USE_MOCK=false).
VITE_API_URL=http://localhost:8000
